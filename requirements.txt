# Ledger — dependencies (verified on Python 3.14)
# Data / ML / viz core
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
plotly>=5.20
# Orchestration + LLM
langgraph>=0.2
langchain-anthropic>=0.2
pydantic>=2.6
python-dotenv>=1.0
# App
streamlit>=1.30
# Modeling (M3) — extras
statsmodels>=0.14      # forecasting (M3+)
shap>=0.45             # optional deeper explanations; permutation importance is the default
# Optional gradient boosters — used automatically IF importable.
# On macOS they need OpenMP:  brew install libomp
# xgboost>=2.0
# lightgbm>=4.0

# Added in later milestones:
#   M4 report export: weasyprint (or browser print-to-PDF)
#   M5 tests: pytest
