# Copy this file to `.env` (same folder) and fill in your key.
#   cp .env.example .env
#
# Optional: Ledger's deterministic data-science core runs WITHOUT this key.
# Setting it turns on the Claude layer — executive narratives, smarter Q&A,
# and LLM-inferred column meanings.
ANTHROPIC_API_KEY=sk-ant-your-key-here
